# Core Dependencies
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0
loguru>=0.7.0
python-dateutil>=2.8.2

# Database
sqlalchemy>=2.0.0

# Scheduling
apscheduler>=3.10.0

# API & Network
requests>=2.31.0
curl_cffi>=0.5.9
pyotp>=2.9.0
python-dotenv>=1.0.0  # Environment variables for secure credential management

# Telegram (choose one - requests is simpler)
# python-telegram-bot>=20.0  # OR just use requests

# Kite Trade (if available, otherwise use existing implementation)
# kite_trade  # May need manual installation or use pi4 implementation

# Kite Connect (for kite_api trade method - optional)
# Install only when switching to kite_api mode
kiteconnect>=5.1.0  # Official Zerodha Kite Connect library (5.1.0+ required for market_protection)

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Development
black>=23.0.0
flake8>=6.0.0
